<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full text-white min-h-screen bg-zinc-900">
      <a class="bg-red-500 p-4 rounded-md" href="/logout">Logout</a>
      <h2 class="text-3xl font-bold text-center text-white pt-10">
        Hello <%= user.name %>
      </h2>
      <p class="text-center p-2">You can create a New post</p>
      <form action="/post" method="post">
        <textarea
          name="content"
          placeholder="Write your post here..."
          class="w-1/3 p-2 resize-none bg-transparent border-2 mt-4 rounded-md mx-auto block"
        ></textarea>
        <input
          type="submit"
          value="Create Post"
          class="block w-1/3 mx-auto my-4 p-2 rounded-md bg-blue-800 text-white placeholder-white-400"
        />
      </form>
      <h1 class="text-3xl my-4 p-4 font-bold text-left text-white pt-10">
        Your Posts.
      </h1>
      <div class="flex justify-between gap-10 p-4 mx-auto">
        <% user.posts.forEach((post)=> {%>
        <div class="p-4 bg-zinc-700 border-2 w-1/3 rounded-md">
          <h1 class="text-blue-200 text-xl">@<%= user.username %></h1>
          <p class="text-gray-300 p-2"><%= post.content %></p>
          <p class="text-sm text-gray-300 p-2">
            <%= post.likes.length %> Likes
          </p>

          <div class="flex btns mt-5 gap-4">
            <a class="text-blue-500" href="/like/<%=post._id%>">
              <%= post.likes.includes(user._id)?"Unlike":"Like" %>
            </a>
            <a class="text-yellow-500" href="/edit/<%=post._id%>">Edit</a>
            <a class="text-red-500" href="">Delete</a>
          </div>
        </div>
        <%}) %>
      </div>
    </div>
  </body>
</html>
